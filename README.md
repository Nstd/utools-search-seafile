# utools-search-seafile
utools插件，搜索seafile中的文件

# 使用步骤
1. 输入`Settings`（Seafile的图标），进入设置界面
2. 添加目录和目录别名以及需要过滤的文件后缀（多后缀由`|`分隔，不过滤可以不填），然后点击保存
3. `Esc`退出设置，然后输入之前设置过的`<别名>`，进行搜索
4. 选择需要打开的目录或文件，按`Enter`打开